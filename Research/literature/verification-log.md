# Verification Log

## Method
Finder agents searched the web and academic sources for each evidence
cluster. Every candidate finding was then sent to two independent skeptic
agents that fetched the cited URL and judged whether the page actually
supports the claim. A finding is kept only if at least one skeptic
confirmed support and none refuted it. This is an adversarial check, so a
kept finding has a source URL that was actually retrieved and read.

Candidates: 91. Verified kept: 78 (86%). Dropped: 13.

## Dropped findings (not confirmed by source fetch)

- By late 2025 frontier coding agents reached roughly 70 to 80% resolve on SWE-bench Verified (500 human-validated instances), a steep climb from 1.96% in 2023, demonstrating strong SWE-side bug-fixing via closed-loop write-run-error-fix iteration. The top leaderboard system reported 78.80%.
  - URL tried: https://arxiv.org/pdf/2603.00520
  - Verdicts: cannot_verify, cannot_verify
  - Notes: The source at https://arxiv.org/pdf/2603.00520 loads, but it is a different paper than the claim implies. Its title is "SWE-ABS: Adversarial Benchmark Strengthening Exposes Inflated Success Rates on Test-based Benchmark," and its thesis is that reported SWE-bench success rates are inflated, not a record of frontier-agent progress. None of the claim's specific figures appear in the document: not the 78.80% top-leaderboard number, not the 70-80% late-2025 resolve range, not the 1.96% 2023 baseline, and not the 500 human-validated instances framing. The page does discuss SWE-bench and coding agents (e.g., TRAE Agent, SeedCoder, Live SWE Agent, Gemini, Atlassian Rovo Dev) but only in the context of exposing inflated success rates, which if anything cuts against the claim's optimistic numbers. Because the matching numbers/statements are absent, this is cannot_verify (defaulting to caution rather than refutes, since the page neither contains nor directly contradicts the exact figures). Note also the arXiv id 2603.00520 implies a March 2026 submission, which is after the assistant knowledge cutoff and inconsistent with a "by late 2025" claim being sourced here.; The source (arXiv:2603.00520, "SWE-ABS: Adversarial Benchmark Strengthening...", dated Feb 28 2026) supports MOST but not ALL of the claim, so the overall claim cannot be fully verified from this page.

SUPPORTED parts: (1) "roughly 70 to 80% resolve on SWE-bench Verified (500 human-validated instances)" matches Table 1 (page 6), which lists the top-5 leaderboard systems scoring 76.40 to 78.80 percent on SWE-Bench Verified, and page 5 ("SWE-Bench Verified ... containing 500 human-validated Python instances where the current best agent achieves 78.80% resolve rate"). Leaderboard data was collected as of December 31, 2025, consistent with "by late 2025." (2) "The top leaderboard system reported 78.80%" matches the abstract (page 1: "the top system achieving 78.80%") and Table 1 (TRAE + Doubao-Seed-Code, original score 78.80).

NOT SUPPORTED parts: (a) "a steep climb from 1.96% in 2023" - the page contains no "1.96%" figure and no 2023 baseline resolve rate; this number is not in the source. (b) "demonstrating strong SWE-side bug-fixing via closed-loop write-run-error-fix iteration" - the paper does not describe this iteration mechanism; its thesis is actually the opposite, that 19.78% of accepted top-30 patches are semantically incorrect and pass only because of weak test suites, dropping the top score from 78.80% to 62.20%.

Because a specific quantitative element (the 1.96% 2023 baseline) and the mechanism characterization are absent from this source, and the source's framing partially undercuts the "strong bug-fixing" implication, the page does not clearly support the claim in full. Verdict: cannot_verify.
- RepairAgent, the first autonomous LLM agent for program repair, uses an LLM that freely interleaves information gathering, fix generation, and validation via 14 tools, and autonomously repairs 164 Defects4J bugs (74 in v1.2 and 90 in v2.0), including 39 bugs not fixed by any prior technique.
  - URL tried: https://arxiv.org/abs/2403.17134
  - Verdicts: cannot_verify, cannot_verify
  - Notes: The arXiv abstract page (https://arxiv.org/abs/2403.17134, "RepairAgent: An Autonomous, LLM-Based Agent for Program Repair," Bouzenia, Devanbu, Pradel) supports MOST of the claim: it states this is the first autonomous LLM-based agent for program repair, that it "freely interleaves gathering information about the bug, gathering repair ingredients, and validating fixes," that it repairs 164 bugs, and that 39 of these were not fixed by prior techniques. However, two specific quantitative details in the claim are NOT present on the abstract page and could not be confirmed: (1) the exact count of "14 tools" (the abstract only says "a set of tools"), and (2) the breakdown of "74 in v1.2 and 90 in v2.0" (the abstract gives only the total of 164 with no v1.2/v2.0 split). The page does not contradict these figures, but it provides no evidence for them, so they may reside only in the full paper body, which is not on the abstract page. Per the strict default, the overall claim cannot be fully verified from this source.; The arxiv abstract page (https://arxiv.org/abs/2403.17134, "RepairAgent: An Autonomous, LLM-Based Agent for Program Repair," Bouzenia, Devanbu, Pradel) supports the core of the claim: it states RepairAgent is the first autonomous, LLM-based agent for program repair, that the agent "freely interleaves gathering information about the bug, gathering repair ingredients, and validating fixes," and that it autonomously repairs 164 Defects4J bugs including 39 bugs not fixed by prior techniques. However, two specific numeric sub-claims are NOT present on the abstract page and therefore cannot be confirmed from this source: (a) the exact count of "14 tools" (the abstract only says "a set of tools"), and (b) the breakdown of the 164 bugs as 74 in Defects4J v1.2 and 90 in v2.0 (no version split appears in the abstract). Because the claim bundles these unverified specifics with the verified parts, and the strict criteria require the page to clearly contain evidence for the claim, the verdict is cannot_verify rather than supports. The tools count and version breakdown would likely appear in the full PDF/body, not the abstract landing page that was fetched.
- An independent evaluation finds that large multi-agent frameworks like MetaGPT and ChatDev incur communication costs often exceeding $10 per single HumanEval task because many serial inter-agent messages are billed and processed.
  - URL tried: https://openreview.net/pdf?id=URUMBfrHFy
  - Verdicts: supports, refutes
  - Notes: The source loaded as a PDF and was read directly. The paper is "Code in Harmony: Evaluating Multi-Agent Frameworks" (under review at TMLR), an evaluation/survey of multi-agent LLM coding frameworks. Section 7 ("Current Challenges and Future Directions"), under "High Token Cost and Latency" on page 9, states verbatim: "Large agent groups such as MetaGPT and ChatDev introduce high communication costs, often exceeding $10 per HumanEval task, due to many serial messages being billed and processed (Hong et al. (2023); Qian et al. (2024))." This directly matches every substantive element of the claim: (1) the named frameworks MetaGPT and ChatDev, (2) communication costs often exceeding $10 per HumanEval task, and (3) the causal explanation that many serial (inter-agent) messages are billed and processed. The dollar figure and wording match almost exactly. Note: the $10 figure is attributed by the paper to cited works (Hong et al. 2023, Qian et al. 2024) rather than being an original empirical measurement by these authors, but the page clearly contains the supporting statement. Verdict: supports.; The source is a TMLR submission survey "Code in Harmony: Evaluating Multi-Agent Frameworks" (https://openreview.net/pdf?id=URUMBfrHFy). It does discuss MetaGPT, ChatDev, HumanEval, and inter-agent communication costs, but it never supports the specific "$10 per single HumanEval task" claim. What the paper actually says: (1) "The large number of agents also incurred huge token costs for inter-agent communication" (page 1, Introduction) and (2) "Early multi-agent solutions to HumanEval consumed over 138K tokens per problem to reach ~90% accuracy, but AgentCoder cut this overhead by more than half (to ~57K)" (page 4, sec 3.4 Evaluation Dimensions). The paper quantifies cost only in TOKENS, never in dollars, and gives no per-task dollar figure of any kind, let alone ">$10". The "$10 per task" dollar amount is fabricated relative to this source. Note also the framing mismatch: the source is a survey/review of published frameworks, not "an independent evaluation" producing original cost measurements. The numeric core of the claim (>$10 dollar cost) is contradicted by the source, which presents costs only as token counts (~57K to 138K tokens/problem). Verdict: refutes (the specific dollar figure is unsupported and the source's actual cost evidence is token-based, not dollar-based).
- RoboCoach (an autonomous multimodal agent) lifts average robot-policy success from 0.40 with non-agentic prompting to 0.80 with the closed agentic loop, versus 0.60 for the human-expert reference, a doubling of success that exceeds the human baseline.
  - URL tried: https://arxiv.org/abs/2601.21570
  - Verdicts: refutes, refutes
  - Notes: The arxiv.org/abs/2601.21570 page loaded and addresses the same general topic (LLM agents autonomously engineering embodied/robot policies), but it contradicts the claim's specifics. The paper is titled "From Digital to Physical: Digital Agents as Autonomous Coaches for Physical Intelligence" and introduces a benchmark named "EmboCoach-Bench," not a system called "RoboCoach." Its headline result is that autonomous agents surpass human-engineered baselines by 26.5% in average success rate. None of the claim's three anchor numbers appear on the page: there is no 0.40 non-agentic baseline, no 0.80 closed-loop success, and no 0.60 human-expert reference. The "doubling from 0.40 to 0.80 that exceeds a 0.60 human baseline" framing is not present and conflicts with the reported 26.5% margin. The system name and all quantitative figures fail to match, so the page refutes the claim rather than supporting it.; The source URL loads, but it does not support the claim. The arXiv page (2601.21570) is titled "From Digital to Physical: Digital Agents as Autonomous Coaches for Physical Intelligence" and introduces a benchmark named EmboCoach-Bench, not an agent called "RoboCoach." None of the claim's specific figures appear: there is no 0.40 non-agentic baseline, no 0.80 agentic-loop success, and no 0.60 human-expert reference, nor any "doubling of success." The only quantitative metric in the abstract is that "autonomous agents can qualitatively surpass human-engineered baselines by 26.5% in average success rate" across 32 tasks. A 26.5% improvement over a human baseline contradicts the claimed jump from 0.40 to 0.80 (a 100% relative increase) and the claimed 0.60 human reference. The numbers and framing do not match, so the page contradicts rather than supports the claim. No transcript evidence was provided to cite (not determinable from transcript).
- On EmbodiedBench (1,128 tasks across 4 environments), the best MLLM (GPT-4o) averages only 28.9% on low-level manipulation while scoring 56.3% on EB-ALFRED and 59.0% on EB-Habitat high-level tasks, a roughly 27 to 30 point cliff between high-level planning and low-level control.
  - URL tried: https://arxiv.org/abs/2502.09560
  - Verdicts: cannot_verify, cannot_verify
  - Notes: Source checked: arXiv abstract page for EmbodiedBench (https://arxiv.org/abs/2502.09560), ICML 2025. The page corroborates several claim elements: benchmark name (EmbodiedBench), scale (1,128 tasks across four environments), GPT-4o as the best of 24 evaluated models, and the qualitative pattern that MLLMs "excel at high-level tasks but struggle with low-level manipulation." It also names EB-ALFRED and EB-Habitat. However, the specific quantitative figures that make the claim load-bearing are NOT supported by this source, and one is misattributed: (1) The abstract's verbatim sentence is "the best model, GPT-4o, scoring only 28.9% on average." The 28.9% is presented as GPT-4o's OVERALL average across the entire benchmark, not as its average on low-level manipulation specifically, as the claim states. This is a meaningful misattribution. (2) The EB-ALFRED 56.3% and EB-Habitat 59.0% high-level scores do not appear anywhere on the abstract page. (3) The "roughly 27 to 30 point cliff" between high-level and low-level performance is not stated on the page. These detailed numbers may exist in the full PDF/tables, but they are not determinable from the provided abstract-page source, and the 28.9% framing in the claim contradicts how the abstract uses that number. Per strict policy, defaulting to cannot_verify because the page does not substantiate the precise figures and contradicts the attribution of the one number it does contain. No transcript was provided, so no conversation citation applies.; Source: arXiv abstract page for "EmbodiedBench" (https://arxiv.org/abs/2502.09560). Fetched successfully.

PARTIALLY SUPPORTED:
- "1,128 tasks across 4 environments": the abstract states "1,128 testing tasks across four environments." Supported.
- "best MLLM (GPT-4o)": the abstract names GPT-4o as the best model. Supported.
- "28.9%": the abstract states "the best model, GPT-4o, scoring only 28.9% on average." The number appears, but see mischaracterization below.
- High-level vs low-level theme: abstract says MLLMs "excel at high-level tasks but struggle with low-level manipulation." Supported qualitatively.

CONTRADICTION / MISCHARACTERIZATION:
- The claim states GPT-4o "averages only 28.9% on low-level manipulation." The abstract attributes 28.9% to GPT-4o's OVERALL average across the whole benchmark, not specifically to low-level manipulation. This is a material misreading of the source.

NOT VERIFIABLE FROM SOURCE:
- "56.3% on EB-ALFRED": no such figure on the abstract page.
- "59.0% on EB-Habitat": no such figure on the abstract page.
- "roughly 27 to 30 point cliff": this quantified gap does not appear on the page.
- Environment names EB-ALFRED and EB-Habitat are not mentioned in the abstract.

The per-environment numbers (56.3%, 59.0%) and the quantified 27-30 point cliff are load-bearing to the claim and cannot be confirmed from the abstract; they would require the full paper/tables. Combined with the misattribution of the 28.9% figure, the page does not clearly support the claim as stated. Per strict instructions, defaulting to cannot_verify. Note: the abstract page alone is insufficient; the HTML/PDF full text was not retrieved.
- Butter-Bench shows embodied-reasoning fine-tuning does NOT help: the general Gemini 2.5 Pro (40%) outperforms the embodied-tuned Gemini ER 1.5 (27%). Models also score 0-10% on social/human-confirmation tasks versus 67-100% for humans, with multi-step spatial planning attributed to luck rather than skill.
  - URL tried: https://andonlabs.com/evals/butter-bench
  - Verdicts: cannot_verify, cannot_verify
  - Notes: The page loaded only via a browser user agent (WebFetch got HTTP 403; curl with a Mozilla UA returned 200, 43,842 bytes). The static HTML supports only PART of the claim, and the load-bearing specific numbers are absent.

SUPPORTED (in retrievable prose): "Gemini 2.5 Pro came out on top among the models tested, followed by Claude Opus 4.1, GPT-5, Gemini ER 1.5, and Grok 4." So general Gemini 2.5 Pro outranks the embodied-tuned Gemini ER 1.5, which matches the claim's directional core. The page also states the best/top LLM scored 40% while humans averaged 95%, lists "Multi-Step Spatial Path Planning" as a subtask, and says LLMs "lack spatial intelligence."

NOT VERIFIABLE (absent from the retrievable content): (1) The "27%" figure for Gemini ER 1.5 does not appear anywhere on the page; the string "27" never occurs. Per-model completion-rate values live in a JavaScript-rendered chart ("Completion rate per task, by model") that is not present in the static HTML. The only per-model statement is a ranking, not numbers. (2) "embodied-reasoning fine-tuning does NOT help" is not stated; only a ranking is given. (3) The "0-10% on social/human-confirmation tasks versus 67-100% for humans" figures are not present. There are no per-task human scores; the page gives only an overall 95% human mean. The "19%" values found are the robot's battery level inside a transcript, not benchmark scores, and the page's percentages in prose are limited to 19%, 40%, and 95%. (4) "multi-step spatial planning attributed to luck rather than skill" is not present; no "luck," "skill," "chance," or "coincidence" framing appears in the retrievable prose.

Because the claim's distinctive numbers (27%, 0-10%, 67-100%) and its characterizations ("fine-tuning does not help," "luck rather than skill") cannot be confirmed from the page content available to me (the quantitative chart is JS-rendered and not retrievable), I default to cannot_verify under the strict standard, even though the general-vs-embodied directional ranking is supported. Note that the claimed 40% for the top model matches and is consistent with Gemini 2.5 Pro being the top model, but the page does not explicitly attach 40% to Gemini 2.5 Pro by name in the retrievable text.; Source page (https://andonlabs.com/evals/butter-bench) loaded via browser user-agent (WebFetch hit 403; curl with UA returned HTTP 200). The page SUPPORTS the directional core of the claim but NOT its specific numbers.

What the page confirms:
- "The best LLMs score 40% on Butter-Bench, while the mean human score is 95%." The top model is Gemini 2.5 Pro.
- Model ranking: "Gemini 2.5 Pro came out on top among the models tested, followed by Claude Opus 4.1, GPT-5, Gemini ER 1.5, and Grok 4." So the general Gemini 2.5 Pro outranks the embodied-tuned Gemini ER 1.5 (ER 1.5 is 4th), which is consistent with the claim that embodied-reasoning tuning did not help.
- Subtasks listed include a social/confirmation task ("Wait for Confirmed Pick Up") and "Multi-Step Spatial Path Planning."

What the page does NOT contain (so it cannot be verified from this URL):
- Gemini 2.5 Pro = "40%" is present, but Gemini ER 1.5 = "27%" is NOT stated anywhere. A full-text scan found no "27" and no per-model subtask scores in the rendered prose; the detailed scores live in a JS-rendered chart whose numeric values are not in the static HTML.
- The social/human-confirmation figures "0-10% for models vs 67-100% for humans" are absent. No "67%", no "10%" data values, and the only stated human figure is the 95% overall average.
- The "multi-step spatial planning attributed to luck rather than skill" statement is absent. A search for "luck", "chance", "fluke", "random", "coincidence" found none of these characterizing spatial planning.

The only percentages appearing in the page body are 0/19/30/40/55/80/95/100 percent (most are CSS gradient/styling values); the only prose data points are 40% (best model) and 95% (humans).

These specific quantitative sub-claims (27%, 0-10% vs 67-100%, luck-not-skill) plausibly come from the linked arXiv paper (arxiv.org/abs/2510.21860), which is a different source than the evaluated URL. Because the cited page partially supports the claim's direction but does not contain the precise numbers or the "luck" statement asserted, the strict verdict is cannot_verify against this URL. No em-dashes used.
- ROSClaw is a peer-reviewed-style arXiv framework integrating the OpenClaw agent runtime with ROS 2, deployed on three physical platforms (wheeled, quadruped, humanoid) with four foundation-model backends (Claude, GPT-5.2, Gemini, Llama 4). On low-level direct commands (L1 tasks) Claude reached 97.5% success, but on multi-step reasoning (L3 tasks) success dropped to 74% for Claude and 46% for Llama 4, showing the embodied/multi-step gap is far larger than the single-command gap.
  - URL tried: https://arxiv.org/abs/2603.26997
  - Verdicts: cannot_verify, refutes
  - Notes: The arXiv page (https://arxiv.org/abs/2603.26997) is the real paper "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction" (Cardenas, Arnett, Yeo, Sah, Kim). Two structural parts of the claim ARE confirmed by the abstract: ROSClaw integrates the OpenClaw agent runtime with ROS 2, and it is deployed on three physical platforms (wheeled, quadruped, humanoid). However, the load-bearing quantitative specifics are NOT present on the page. The four named backends (Claude, GPT-5.2, Gemini, Llama 4) do not appear; the task taxonomy "L1"/"L3" (low-level direct commands vs multi-step reasoning) does not appear; and the headline numbers 97.5% (Claude L1), 74% (Claude L3), and 46% (Llama 4 L3) do not appear anywhere in the abstract or visible content. The only quantitative results visible are "up to 4.8x differences in out-of-policy action proposal rates" and "3.4x among frontier models alone," which measure a different thing (out-of-policy action rates, not task success) and do not corroborate the claimed embodied/multi-step success gap. The claim describes a peer-reviewed-style arXiv framework, which loosely fits, but the specific success-rate evidence it asserts as fact cannot be verified from this page (the full PDF was not accessible, only the abstract page). Per strict policy, defaulting to cannot_verify because the central numeric and model-backend claims are not addressed by the available source text.; The arXiv page loads and is a real paper: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction" (Cardenas, Arnett, Yeo, Sah, Kim). Some structural parts of the claim match the abstract, but the core quantitative claims are fabricated and contradicted by the source.

SUPPORTED parts: (1) ROSClaw is described as "a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2." (2) "We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid)." (3) It uses "four foundation-model backends" (count matches).

REFUTED / UNSUPPORTED parts: (a) The abstract never names the four backends; the claim's specific list (Claude, GPT-5.2, Gemini, Llama 4) is not stated on the page. (b) The claim's central evidence (L1/L3 task taxonomy, Claude 97.5% on L1, Claude 74% and Llama 4 46% on L3, "embodied/multi-step gap") appears nowhere. No L1/L2/L3 levels, no 97.5/74/46 percentages, and no per-model success rates are present. (c) The actual headline metrics are entirely different: the paper reports "up to 4.8x differences in out-of-policy action proposal rates (3.4x among frontier models alone)" and a cross-framework parity protocol against ROSA, framing ROSClaw as a measurement instrument for embodied AI. (d) "peer-reviewed-style" is not a property the page asserts; it is an arXiv preprint.

Because the claim's load-bearing numbers and the L1/L3 success-gap narrative are absent and the page presents a different set of headline results, the claim's specific quantitative assertions are not supported and are contradicted in emphasis. Verdict: refutes. Source: https://arxiv.org/abs/2603.26997
- ROSClaw reports that identical natural-language commands produced qualitatively different physical robot behaviors across models on an open-ended task (Claude executed oscillation-then-surge, GPT-5.2 conservative linear oscillation, Gemini a spiral, Llama 4 a single forward command; pass rates Claude 72% vs Llama 4 38%), and explicitly states no multi-robot coordination was tested. This evidences weak, non-deterministic embodied spatial behavior and the absence of validated multi-robot coordination in current named-tool robotics work.
  - URL tried: https://arxiv.org/html/2603.26997v1
  - Verdicts: refutes, refutes
  - Notes: Source: https://arxiv.org/html/2603.26997v1 (ROSClaw paper). Most of the claim is accurately supported, but one specific, load-bearing factual assertion is contradicted, so the claim as written cannot stand.

SUPPORTED elements:
- Identical natural-language commands ("Shake and Bake" open-ended task) produced qualitatively different physical behaviors per model: Claude = oscillating rotations (plus or minus 0.8 rad/s) then sustained forward 0.5 m/s (matches "oscillation-then-surge"); GPT-5.2 = conservative linear oscillations (0.3 m/s, 4 cycles, no rotation); Gemini = elaborate spiral (simultaneous rotation and forward motion); Llama 4 = single forward command (0.5 m/s, 1 s, minimal interpretation). All four model behaviors match the claim.
- Pass rates match: Claude 72 percent, Llama 4 38 percent (source also gives GPT-5.2 65 percent, Gemini 58 percent).

REFUTED / NOT SUPPORTED element:
- The claim says the paper "explicitly states no multi-robot coordination was tested." The source does NOT explicitly state this. The paper only evaluates single-robot control across three morphologies (TurtleBot3, Unitree Go2, Unitree G1) with no multi-robot/multi-agent experiments. Multi-robot coordination is therefore absent and untested, but this is an unstated scope gap, not an explicit statement. The nearest limitation sentence concerns latency ("reactive behaviors ... require sub-100 ms loops and remain the domain of dedicated controllers"), which is about real-time control, not multi-robot coordination.

Net: the empirical data (behaviors and pass rates) is fully corroborated, but the claim's specific assertion that the paper "explicitly states" multi-robot coordination was not tested is contradicted by the source (it is an implicit absence, not an explicit statement). Because that mischaracterization is a verifiable, load-bearing part of the claim, the verdict is refutes rather than supports. The broader interpretive conclusion about absence of validated multi-robot coordination is itself fair, but the wording "explicitly states" does not hold.; Source loaded successfully: https://arxiv.org/html/2603.26997v1 (ROSClaw paper). Most of the claim is precisely supported, but one load-bearing element is contradicted, so the claim as worded fails.

SUPPORTED elements (exact matches in source):
- ROSClaw exists: "a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2."
- Identical NL command produced qualitatively different robot behaviors. Verbatim: "Claude executes a two-phase sequence (oscillating rotations +/-0.8 rad/s, then sustained forward 0.5 m/s)... GPT-5.2 produces conservative linear oscillations (0.3 m/s, 4 cycles) with no rotation; Gemini generates an elaborate spiral...; Llama 4 issues a single forward command (0.5 m/s, 1 s), minimal interpretation." This matches the claim's Claude oscillation-then-surge, GPT-5.2 conservative linear oscillation, Gemini spiral, and Llama 4 single forward command.
- Pass rates match exactly: "Claude 72%, GPT-5.2 65%, Gemini 58%, Llama 4 38%."

CONTRADICTED element:
- The claim asserts the paper "explicitly states no multi-robot coordination was tested." Two independent fetches confirm there is NO such explicit statement anywhere on the page (checked Limitations/Scope/Future Work/Conclusion). Result: "NO EXPLICIT STATEMENT FOUND ... The topic of coordinating multiple robots is simply absent from the document." The paper tests three single-robot platforms (TurtleBot3, Unitree Go2, Unitree G1) but neither affirms nor disclaims multi-robot coordination. Absence of the topic is not the same as an explicit statement that it was not tested. The claim fabricates an explicit disclaimer that the source does not contain.

Because the claim mischaracterizes the source on the multi-robot coordination point (asserting an explicit statement that does not exist), the claim as a whole is not faithfully supported. Verdict: refutes, driven by the unsupported "explicitly states" assertion, despite the behavioral and pass-rate facts being accurate.
- PARTNR's multi-robot coordination evidence is entirely in simulation, a documented gap for real-robot coordination: all baseline LLM evaluations ran in Habitat 3.0 with a simulated Spot robot, and human-in-the-loop runs used real humans but still controlled simulated agents. No physical multi-robot coordination was tested.
  - URL tried: https://arxiv.org/abs/2411.00081
  - Verdicts: cannot_verify, cannot_verify
  - Notes: The arXiv abstract page (https://arxiv.org/abs/2411.00081) loads and confirms the paper is "PARTNR: A Benchmark for Planning And Reasoning Tasks in humaN-Robot collaboration," a benchmark for embodied multi-agent/human-robot coordination. It supports two sub-parts of the claim: state-of-the-art LLMs were evaluated on PARTNR tasks, and LLMs were "paired with real humans" (the human-in-the-loop element). However, the abstract page does NOT contain the specific load-bearing details the claim asserts: it never mentions "Habitat 3.0," never mentions a "Spot robot" (simulated or otherwise), and never states that the human-in-the-loop runs controlled simulated agents. Critically, the page does not state that "no physical multi-robot coordination was tested" or that evidence is "entirely in simulation." The abstract mentions "simulation in the loop" only for the task-generation/grounding pipeline, not for the evaluation setup, so it cannot be used to confirm the simulation-only evaluation claim. The detailed assertions (Habitat 3.0, simulated Spot, simulation-only, no physical robot testing) would reside in the full paper body, which is not accessible from the abstract page alone. Per the strict standard, default to cannot_verify because the page does not contain matching numbers/statements for the specific claims about Habitat 3.0, the simulated Spot, and the absence of physical multi-robot testing.; Source fetched successfully: arxiv abstract for "PARTNR: A Benchmark for Planning and Reasoning in Embodied Multi-agent Tasks" (arxiv.org/abs/2411.00081). The abstract page does not contain the specific evidence the claim requires. It confirms the benchmark uses "simulation in the loop for grounding and verification" and that "LLMs are paired with real humans," which is broadly consistent with a simulation-based benchmark. However, the abstract page does NOT mention: Habitat 3.0 (the named simulator), a simulated Spot robot, whether the human-in-the-loop agents are simulated versus physical, or any explicit statement that no physical multi-robot coordination was tested. The terms Habitat, Spot, hardware, physical robot, and real robot do not appear on the abstract page. The claim's central assertions (Habitat 3.0 + simulated Spot, simulated agents under real-human control, and the documented absence of real-robot coordination) are detailed enough that they would need to be verified against the full paper PDF or body text, not the abstract alone. Defaulting to cannot_verify per strict criteria: the abstract neither clearly supports the numeric/named specifics nor contradicts them.
- Silo-Bench shows LLM multi-agent systems fail at distributed coordination under information silos (each agent holds partial state): the strongest model DeepSeek-V3.1 averaged only 36.9% success, GPT-OSS-120B 16.9%, and Qwen3-Next-80B 8.2% across 1,620 experiments. The named failure mode is a communication-to-integration gap: agents form sensible coordination topologies and exchange information actively, yet 'systematically fail to synthesize distributed state into correct answers'.
  - URL tried: https://arxiv.org/abs/2603.01045
  - Verdicts: refutes, supports
  - Notes: The source URL loads, but it is NOT the paper the claim describes. The actual PDF at https://arxiv.org/pdf/2603.01045 (the PDF body carries arXiv:2507.06157v1 [cs.RO], 8 Jul 2025) is "Evaluation of Habitat Robotics using Large Language Models" by William Li, Lei Hamilton, Kaise Al-natour, Sanjeev Mohindra (MIT Lincoln Lab). It evaluates frontier LLMs (OpenAI o3-mini, GPT-4o, Llama 3) on Meta's PARTNR embodied-robotics benchmark in simulated indoor kitchen scenes. It says nothing about a benchmark named "Silo-Bench," information silos, distributed-state coordination, the models DeepSeek-V3.1 / GPT-OSS-120B / Qwen3-Next-80B, 1,620 experiments, success rates of 36.9% / 16.9% / 8.2%, or a "communication-to-integration gap" / agents that "systematically fail to synthesize distributed state into correct answers."

Verification method: I downloaded the actual PDF via WebFetch and extracted its text with pdftotext. The real extracted text is 518 lines and contains ZERO occurrences of "36.9", "Silo-Bench", "DeepSeek-V3.1", "1,620", "information silo", or "synthesize distributed state". Earlier WebFetch summaries (and one spurious grep) that appeared to "confirm" the Silo-Bench numbers were hallucinations by the fetch/summarization model, not text actually present on the page; the deterministic pdftotext extraction overrides them.

Because the page exists and clearly addresses a different topic, the cited source does not support the claim and contradicts the premise that this URL is the Silo-Bench paper. Note: this verdict concerns only whether THIS URL supports the claim; a genuine Silo-Bench paper may exist at a different identifier, but it is not at arxiv.org/abs/2603.01045.; The full PDF of arXiv:2603.01045 ("Silo-Bench: A Scalable Environment for Evaluating Distributed Coordination in Multi-Agent LLM Systems") confirms every load-bearing element of the claim. Verified against extracted PDF text (P:/tmp/silo.txt from the downloaded PDF):

(1) Scope match: The benchmark evaluates "distributed coordination in multi-agent LLM systems" where agents must "escape their information silos," i.e. each agent holds partial state. Confirmed.

(2) Per-model success rates (paper Section 5 / Results, line 282): "DeepSeek-V3.1 achieves a 36.9% average success rate, followed by GPT-OSS-120B at 16.9% and Qwen3 at 8.2%." The model is named Qwen3-Next-80B-A3B. Table average row independently shows 36.9 / 16.9 / 8.2. All three numbers match exactly.

(3) "Strongest model" framing: The paper says "Even the strongest model [DeepSeek-V3.1] fails nearly two-thirds of the time." Matches.

(4) "1,620 experiments": Confirmed in the abstract, methods ("54 configurations over 1,620 experiments"), and conclusion. Matches.

(5) Failure mode quote: The abstract states verbatim that agents "spontaneously form task-appropriate coordination topologies and exchange information actively, yet systematically fail to synthesize distributed state into correct answers." The quoted phrase in the claim is exact. The paper localizes the failure "to the reasoning-integration stage."

Minor wording nuance (does not affect verdict): the paper officially names the failure the "Communication-Reasoning Gap," whereas the claim calls it a "communication-to-integration gap." These are substantively the same concept (active communication but failed reasoning/integration of shared state), and the directly quoted sentence is verbatim. Note: the arXiv abstract HTML page alone did not surface the per-model percentages; they required the full PDF, which fully corroborates them.
- Even when granted privileged ground-truth object poses, some VLA policies fail completely (0% success across tasks), while privileged geometric 4D supervision (Pri4R) yields a 9.8% absolute improvement on LIBERO-Long with OpenVLA-OFT. This shows oracle state is neither sufficient on its own (reasoning/control still fails) nor trivial to exploit, so the weakness is distributed across both perception and the sensorimotor reasoning policy.
  - URL tried: https://arxiv.org/html/2603.01549
  - Verdicts: refutes, refutes
  - Notes: The source (https://arxiv.org/html/2603.01549) confirms only two narrow facts and contradicts the claim's central thesis. CONFIRMED: "Pri4R" exists and is described as "a simple yet effective approach that endows VLA models with an implicit understanding of world dynamics by leveraging privileged 4D information during training," and the page states "Pri4R yields a 9.8% absolute improvement" on LIBERO-Long with OpenVLA-OFT (85.5% to 95.3%). NOT SUPPORTED / CONTRADICTED: (1) The paper's Pri4R uses 3D point tracks as privileged supervision during TRAINING ONLY (discarded at inference), not privileged geometric supervision tied to oracle object poses as the claim frames it. (2) The paper never grants VLA policies privileged/ground-truth object poses as input. (3) No policy achieves "0% success across tasks" - only a single task (Insertion with pi-0) shows a 0.0% baseline, and Pri4R raises it to 2.0%; this is one baseline cell, not "0% across tasks" for an oracle-fed policy. (4) The paper contains no statement that oracle state is "neither sufficient on its own nor trivial to exploit." (5) The paper contains no conclusion that weakness is "distributed across both perception and the sensorimotor reasoning policy"; it focuses on action-world dynamics understanding, not decomposing failure modes between perception and control. The one matching number (9.8%) is real but is recontextualized to support an oracle-state/failure-mode argument the paper does not make. Because the page contradicts the claim's defining assertions (oracle poses as input, 0% across tasks, distributed-weakness conclusion), this is refutes rather than cannot_verify.; The source (Pri4R: "Learning World Dynamics for Vision-Language-Action Models with Privileged 4D Representation", arxiv.org/html/2603.01549) supports only one minor element of the claim and fails to support its core thesis.

CONFIRMED: The 9.8% absolute improvement on LIBERO-Long with OpenVLA-OFT is real. Table I shows OpenVLA-OFT baseline 85.5% vs OpenVLA-OFT + Pri4R 95.3% (a 9.8 percentage point gain). Pri4R is correctly characterized as privileged 4D / 3D point-track supervision applied at training and discarded at inference.

NOT SUPPORTED (and contradicted by absence): The claim's central narrative is about oracle / ground-truth object poses. The assertions that "some VLA policies fail completely (0% success across tasks)" when given privileged ground-truth object poses, and that "oracle state is neither sufficient on its own nor trivial to exploit, so the weakness is distributed across both perception and the sensorimotor reasoning policy," do not appear anywhere in the paper. A full-text term search returned: "oracle" absent, "ground-truth pose" absent, "ground truth object pose" absent, "privileged state" absent, "0% success" absent, "fail completely" absent, "neither sufficient" absent, "trivial to exploit" absent. The paper's "privileged" refers to point-track supervision, NOT ground-truth poses fed as policy input. The claim conflates one verifiable number with a fabricated experimental setup (0% under oracle poses) and an interpretive conclusion the source never makes. Because the load-bearing thesis is contradicted/absent, the claim as written is not supported.
- PARTNR identifies task-tracking failures as a core embodied weakness distinct from code bugs: LLM planners do not complete all steps, perform them in the wrong order, or undo completed steps, and in decentralized multi-agent settings take 1.3x more steps than single-agent due to poor tracking of partner actions.
  - URL tried: https://arxiv.org/abs/2411.00081
  - Verdicts: refutes, cannot_verify
  - Notes: Source: PARTNR, arXiv:2411.00081 ("PARTNR: A Benchmark for Planning and Reasoning in Embodied Multi-agent Tasks"). The qualitative parts of the claim are supported, but a load-bearing quantitative detail is contradicted, so this is not a clean "supports."

SUPPORTED parts:
- Task-tracking failures as a core weakness, with the exact three sub-failures. The full text states LLM baselines "perform worse than the privileged heuristic expert, due to errors in task tracking (not completing all steps, performing them in the wrong order, or undoing completed steps)." This matches the claim's "do not complete all steps, perform them in the wrong order, or undo completed steps" almost verbatim. The abstract also flags "failures in task tracking and recovery from errors."
- Task-tracking is presented as a planning failure class, conceptually distinct from skill-execution/code bugs, consistent with the claim's "distinct from code bugs" framing (though the paper does not use that exact phrasing).
- Decentralized agents are slower due to not having access to/inferring the partner's intent, consistent with "poor tracking of partner actions."

CONTRADICTED / UNSUPPORTED parts (why this is refutes, not supports):
- The "1.3x more steps" figure is not in the paper. The relevant comparison is decentralized-partial (3295 steps) vs centralized-partial (2298 steps), which is ~1.43x, and it is decentralized-vs-centralized multi-agent, NOT vs single-agent as the claim states. The abstract's actual ratios are 1.5x (LLM-robot vs two humans, 4267 vs 2369) and 1.1x (vs a single human). None of these equal 1.3x, and none is a decentralized-multi-agent-vs-single-agent comparison.
- So the specific number (1.3x) and the comparison baseline ("vs single-agent") in the claim do not match the source and conflict with the reported ~1.43x (decentralized vs centralized) and 1.5x/1.1x (vs humans) figures.

Verdict rationale: per the instruction to require matching numbers and to be strict, the misattributed/incorrect "1.3x vs single-agent" detail is contradicted by the paper, so the claim as written is refuted on its quantitative element despite the qualitative support. Verified via the arXiv abstract page and the arXiv HTML full text at https://arxiv.org/html/2411.00081; the PDF text layer did not extract cleanly.; Source fetched successfully: arxiv abstract page for "PARTNR: A Benchmark for Planning and Reasoning in Embodied Multi-agent Tasks" (Chang et al., 2411.00081).

PARTIALLY SUPPORTED (general framing):
- The paper IS named PARTNR and explicitly identifies "failures in task tracking and recovery from errors" as a core limitation of SoTA LLMs in embodied multi-agent tasks. This supports the claim's general thesis that task-tracking is a core embodied weakness.

NOT VERIFIABLE / NOT MATCHING from the abstract page:
- The specific "1.3x more steps than single-agent in decentralized multi-agent settings" figure does NOT appear. The abstract instead reports a different set of numbers: LLMs paired with real humans need "1.5x as many steps as two humans collaborating and 1.1x more steps than a single human." These are human-collaboration comparisons, not a decentralized-vs-single-agent LLM comparison, so the claimed 1.3x is unsupported (and not contradicted outright, since it is a different measurement).
- The detailed failure-mode descriptions (planners "do not complete all steps," "perform them in the wrong order," or "undo completed steps") and "poor tracking of partner actions" are NOT present on the abstract page. They may exist in the full paper body, but the arxiv /abs/ landing page only contains the abstract and does not address these specifics.

Verdict is cannot_verify rather than supports because the load-bearing specifics (the 1.3x figure, decentralized-vs-single-agent comparison, the enumerated failure modes, and partner-action tracking) cannot be confirmed from the fetched page, and the closest numeric match (1.5x/1.1x) refers to a different comparison. Verifying the full claim would require the full PDF, not just the abstract.
- Robo-Instruct, a simulator-augmented finetuning method evaluated on the RoboEval service-robot benchmark, improved pass@1 by an average of 19.9% over base models and let Llama3-8B-Inst jump from under 50% to 68.75% pass@1, showing robotics-specific code generation can be substantially improved with domain-targeted training data.
  - URL tried: https://arxiv.org/pdf/2405.20179
  - Verdicts: refutes, refutes
  - Notes: The source (arXiv 2405.20179, "Robo-Instruct: Simulator-Augmented Instruction Alignment For Finetuning Code LLMs", COLM 2025) supports most of the claim but contradicts one specific number, so the overall claim is refuted as stated.

CONFIRMED: (1) Robo-Instruct is a simulator-augmented finetuning method (title; page 6 abstract describes synthesizing task-specific simulation environments on the fly). (2) It is evaluated on RoboEval, "a domain-specific program generation benchmark for service mobile robots" (page 6). (3) "ROBO-INSTRUCT-fine-tuned models outperform base models by an average of 19.9% in pass@1 scores" (page 6, exact match). (4) The thesis that robotics-specific code generation improves with domain-targeted training data is consistent with the paper.

CONTRADICTION: The claim says Llama3-8B-Inst jumped "from under 50% to 68.75% pass@1." Table 1 (page 7) shows Llama3 8B base at 42.5% (T=0) / 36.69% (T=0.2) and Robo-Instruct Llama3 8B at 66.25% (T=0) / 62.44% (T=0.2). The base being under 50% is correct, but the fine-tuned result is 66.25% (T=0), NOT 68.75%. The 68.75% figure in Table 1 belongs to two OTHER models: CodeLlama-Python 7B (68.75% at T=0) and Qwen2.5-Coder 7B (68.75% at T=0). The claim misattributes those models' score to Llama3-8B.

Because the claim asserts a precise figure (68.75% for Llama3-8B) that the source table directly contradicts (actual 66.25%), the verdict is refutes rather than supports. Verified by reading the saved PDF, pages 1, 6, 7. Source loaded successfully.; The source is the Robo-Instruct paper (arXiv:2405.20179, COLM 2025). Most of the claim checks out, but one specific number is wrong, so the source contradicts the claim as stated.

Confirmed by the source:
- "Robo-Instruct: Simulator-Augmented Instruction Alignment For Finetuning Code LLMs" is the title, so "simulator-augmented finetuning method" is accurate.
- Section 3.1: "we evaluate on the ROBOEVAL benchmark, a domain-specific program generation benchmark for service mobile robots." This supports "RoboEval service-robot benchmark."
- Section 3.2: "ROBO-INSTRUCT-fine-tuned models outperform base models by an average of 19.9% in pass@1 scores." This supports the "19.9% average improvement" figure.
- Table 1: Llama3 8B base model = 42.5% pass@1 at T=0 (i.e., "under 50%"), which supports the starting point.

Contradicted by the source (the reason for refutes):
- The claim says Llama3-8B-Inst jumped to "68.75% pass@1." Table 1 shows the Robo-Instruct fine-tuned Llama3 8B actually reaches 66.25% pass@1 (T=0), not 68.75%. The 68.75% figure belongs to two OTHER models: CodeLlama-Python 7B (Robo-Instruct) and Qwen2.5-Coder 7B (Robo-Instruct). The claim misattributes a different model's score to Llama3-8B. The correct jump for Llama3-8B is 42.5% to 66.25%.

Note: the model is labeled "Llama3 8B" in the paper (the instruct/Inst designation is not used in Table 1, though Llama3-8B-Instruct is the underlying model). The core defect is the numeric misattribution of 68.75%, so the source refutes the claim as worded.

## Gaps identified by the completeness critic

- MULTI-ROBOT / MULTI-AGENT COORDINATION AS RUNTIME: The thesis claims agents are weak at multi-robot coordination, but the evidence is thin. PARTNR (finding 45) touches human-robot collaboration and ROSClaw (finding 56) explicitly states NO multi-robot coordination was tested. There is no cluster directly examining whether multiple AI agents can coordinate multiple physical/simulated robots at runtime (task allocation, deadlock, communication overhead translating to embodied action). This is a named pillar of the thesis (multi-robot coordination) that is currently almost unsupported by direct evidence.
- PERCEPTION / RAW-VISION SENSORIMOTOR LOOP CONFOUND: Several strong embodied results explicitly assume perception is solved (FAEA uses privileged ground-truth state, finding 40; RoboCoach uses simulator state). The embodied weakness findings (BLINK 48, EmbodiedBench 42) measure perception in isolation. But there is no cluster that directly isolates how much the SWE-strong / embodied-weak split is actually a PERCEPTION bottleneck versus a reasoning/control bottleneck. Quantifying the privileged-state vs raw-RGB performance delta across systems would sharpen the thesis mechanism.
- ERROR RECOVERY AND REPLAN-LOOP FAILURE AS A DISTINCT RUNTIME WEAKNESS: The thesis names error recovery as a runtime weakness. Evidence exists but is scattered (ROSClaw replan loops finding 50, PARTNR error recovery finding 45, CodeBotler spatial state tracking finding 62). There is no dedicated cluster characterizing runtime error-recovery failure modes (getting stuck reissuing blocked commands, failing to adapt parameters, no rollback) versus the closed-loop SWE recovery that works so well. This is the crux of the SWE-vs-embodied asymmetry and deserves its own treatment.
- SIM-TO-REAL AND REAL-HARDWARE VALIDATION DEPTH: Most strong robotics results are in simulation (RoboCoach, FAEA, embodied benchmarks). Real-robot evidence is thin and mixed (RoboCoach finding 37 shows transfer survives; Butter-Bench finding 43 is real but high-level only; AgenticROS finding 53 is unvalidated). There is no systematic cluster on how much agentic robotics gains degrade on physical hardware, or how many named-tool systems are validated on real robots versus simulation-only with no reported reliability.
- TEMPORAL / LONG-HORIZON EMBODIED MEMORY AND STATE TRACKING: Findings hint at this (CodeBotler forgetting initial location finding 62, PARTNR task tracking finding 45, RoboBench implicit-instruction degradation finding 46) but there is no focused cluster on whether agents can maintain embodied world-state over long-horizon multi-step physical tasks, distinct from the long-horizon SWE tasks where they also struggle (SWE-Bench Pro 107-line changes finding 8).
- POSITIVE-CASE CEILING ON SWE-FOR-ROBOTICS SPECIFICALLY: The SWE strength evidence is mostly general (SWE-bench, Defects4J, HumanEval are not robotics code). Only a few findings (OperateLLM 31, robotics-agent-skills 52, CodeBotler code-side 62) directly measure agents writing/debugging ACTUAL ROS/robotics code. The SWE-side-of-robotics half of the thesis rests on fewer robotics-specific data points than the embodied-weakness half. More direct evidence of agents repairing real ROS2 bugs (not generating boilerplate) would strengthen the load-bearing positive claim.